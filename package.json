{
  "name": "@rifo/get-ts-imports",
  "version": "1.0.0",
  "description": "Get typescript imports from file or directory",
  "main": "./index.ts",
  "scripts": {
    "start": "nodemon --watch ./src/index.ts",
    "build": "npx rimraf ./dist && tsc && copyfiles -f ./src/README.md dist && copyfiles -f ./src/package.json dist"
  },
  "author": "Ofir Fridman",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.15",
    "ts-morph": "^14.0.0",
    "ts-node": "^10.6.0",
    "typescript": "^4.6.2"
  }
}

